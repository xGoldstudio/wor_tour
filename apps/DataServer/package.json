{
  "name": "data_server",
  "type": "module",
  "scripts": {
    "clean": "rimraf .dist",
    "build": "pnpm clean && esbuild src/index.ts --bundle --platform=node --target=node18.16.0 --outfile=.dist/index.cjs",
    "dev": "tsx src/index.ts --watch",
    "lint": "eslint . --ext ts,tsx --report-unused-disable-directives --max-warnings 0"
  },
  "dependencies": {
    "@repo/types": "workspace:*",
    "@fastify/cors": "^9.0.1",
    "@fastify/multipart": "^8.2.0",
    "@fastify/static": "^7.0.4",
    "fastify": "^4.27.0",
    "tsx": "^4.11.0",
    "uuid": "^9.0.1"
  },
  "devDependencies": {
    "esbuild": "^0.21.4",
    "@repo/eslint-config": "workspace:*",
    "@repo/typescript-config": "workspace:*",
    "@types/uuid": "^9.0.8",
    "rimraf": "^5.0.7"
  }
}
