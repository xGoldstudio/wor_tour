import { StateAction } from "../CardStatesData";

const BannerOfCommandStateAction: StateAction = ({ clock, event, card, gameState }) => {
	// update attack speed to make it attackFrame remaining
};

export default BannerOfCommandStateAction;