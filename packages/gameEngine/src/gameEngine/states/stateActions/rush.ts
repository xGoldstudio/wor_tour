import { StateAction } from "../CardStatesData";

const RushStateAction: StateAction = ({ clock, event, card, gameState }) => {
	// update attack speed to make it attackFrame remaining
};

export default RushStateAction;